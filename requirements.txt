requests>=2.28
pandas>=1.5
numpy>=1.23
matplotlib>=3.6
# Optional, only needed for pyisda.sasa and pyisda.ligand's binding-site detection:
# biotite>=0.38
# Optional, only needed for pyisda.interactive (HTML/Plotly plots):
# plotly>=5.18
# Optional, only needed for pyisda.viewer (MolViewSpec/Mol* 3D structure viewer):
# molviewspec>=1.0
# ipython  (already present in any Jupyter/Colab environment)
